FILE PERMISSION
  FILE FIND=> find / -name 'my.cnf'
 FILE OWNER => chown -Rv username somedir
 PROCESSOR/MEMORY => htop or top
 PROCESS => ps -ef
 PORTS => netstat -ln
  
 
 Ubuntu
 ------
 
 APT UPDATE => apt-get update (Update the cache). 
 APT CACHE => a
 
 
 USER ADD => sudo adduser username
 USER PASS STATUS => chage -l capman
 USERS LIST => cat /etc/passwd
 
 PERM CHANGE OWNER/GROUP => chown jenkins:adm netxstudio.updatesite/ -R (Owner=jenkins, Group=adm). 
 
 
 SYSLOGGING
 ----------
 
 http://www.linuxvoodoo.com/resources/howtos/syslog
 
 
 Sudo
------

